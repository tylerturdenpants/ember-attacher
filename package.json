{
  "private": true,
  "packageManager": "pnpm@12.5.1",
  "workspaces": [
    "addon",
    "test-app"
  ],
  "name": "ember-attacher",
  "scripts": {
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
