{{#ember-popper class=class
                eventsEnabled=false
                id=id
                modifiers=_modifiers
                onFoundTarget=(action 'onFoundTarget')
                placement=placement
                popperContainer=popperContainer
                renderInPlace=renderInPlace
                target=target as |emberPopper|}}
  {{#ember-attacher-inner animation=animation
                          arrow=arrow
                          class="inner"
                          disableEventListeners=emberPopper.disableEventListeners
                          enableEventListeners=emberPopper.enableEventListeners
                          hideDelay=hideDelay
                          hideDuration=hideDuration
                          hideOn=hideOn
                          interactive=interactive
                          isOffset=isOffset
                          isShown=isShown
                          onChange=onChange
                          scheduleUpdate=emberPopper.scheduleUpdate
                          showDelay=showDelay
                          showDuration=showDuration
                          showOn=showOn
                          target=_target as |inner|}}
    {{yield (hash emberPopper=emberPopper
                  hide=inner.hide)}}
  {{/ember-attacher-inner}}
{{/ember-popper}}
